# 通通办公助手・难点排坑全记录

> 这份文档汇总项目历次调试中**真正卡住过、值得讲**的问题。
> 按"重点详写 + 历史索引 + 轻量优化"三层组织，主次分明。
> 详细的前端踩坑细节请看 `@/Users/leila/Documents/coding/tongtong/docs/前端难点笔记.md`，启动报错请看 `@/Users/leila/Documents/coding/tongtong/docs/后端启动手册.md`。

---

## 〇、一句话故事线

> "项目里**真正花时间排查的难点**有四个：
> ① MiniMax 订阅套餐 key 被误用到 OpenAI 兼容协议接口连续 401；
> ② LangChain 的 `StrOutputParser` 会静默丢掉 thinking 块导致思考链 UI 永远空白；
> ③ macOS 系统 SOCKS 代理污染了 OpenAI/MiniMax SDK 的 HTTP 直连；
> ④ 首次推 GitHub 之前的多层敏感文件审查。
> 其它都是常规的前端响应式坑、CSS 布局、流式协议适配——单个简单，但堆在一起会让你怀疑'问题到底在哪一层'，所以排查方法论比单点知识更重要。"

---

## 一、问题分级地图

| 类别 | 问题 | 难度 | 章节 |
|---|---|---|---|
| 🔴 **核心难点** | MiniMax `sk-cp-` key 401 | 🔥🔥🔥 | §二 |
| 🔴 **核心难点** | StrOutputParser 静默丢 thinking | 🔥🔥🔥 | §三 |
| 🔴 **核心难点** | SOCKS 代理污染 LLM 直连 | 🔥🔥 | §四 |
| 🟡 **架构决策** | 后端多依赖优雅回退（DB / 向量库 / Embedding / 业务接口） | 🔥🔥 | §五 |
| 🟢 **安全工程** | 首次推 GitHub 脱敏 | 🔥🔥 | §六 |
| 🔵 **历史前情** | 前端流式 / 响应式 / 协议 / 鉴权 13 个坑 | 🔥 | §七（索引） |
| ⚪ **轻量优化** | 思考链折叠 + 打字光标 + 时间戳布局 | 🟢 | §八 |

---

## 二、🔴 核心难点 1：MiniMax `sk-cp-` key 401（协议错配）

### 2.1 现象

`.env` 里：
```
OPENAI_API_KEY=sk-cp-...QH4zMg
OPENAI_BASE_URL=https://api.minimaxi.com/v1
OPENAI_MODEL=abab6.5s-chat
```

后端报：
```json
{"error":{"type":"authorized_error","message":"invalid api key (2049)","http_code":"401"}}
```

### 2.2 排查方法论：列假设空间，不要直接下结论

看到 401 的第一反应**不是**"key 不对"——这种结论无信息量。先把假设空间列清：

| 候选假设 | 怎么证伪 |
|---|---|
| H1 key 字符串拼写错 | 复制粘贴对照原文 |
| H2 网络打不到 MiniMax | 看返回是不是 MiniMax 自己的 `request_id` |
| H3 base_url 域名错（国内打到海外） | 同时打多个候选域名对比 |
| H4 模型名 `abab6.5s-chat` 已下线 | 错误码会是 `model not found` 而不是 `invalid api key` |
| H5 **协议错配——key 不属于这个接口** | 排除前 4 项后必然指向这个 |

写探针脚本同时打三个候选域名：

```python
candidates = [
    "https://api.minimaxi.com/v1",   # 国内 OpenAI 兼容
    "https://api.minimax.io/v1",     # 海外 OpenAI 兼容
    "https://api.minimax.chat/v1",   # 老海外域名
]
```

→ 三个域名都返回 401、且都带 MiniMax 自家的 `request_id`。**同时排除 H1-H4，定位到 H5**。

### 2.3 根因：MiniMax 的双 key 体系

MiniMax 控制台接口密钥页面顶部红字：

> 如果您想要按量计费，则使用此 API Key。如果您想通过 **Token Plan** 来使用，则需要专门使用 Token Plan 下的 API Key。

| 区段 | 前缀 | 用途 | 协议 | 入口 |
|---|---|---|---|---|
| 体验中心 | `sk-api-...` | 按量计费 | **OpenAI 协议** | `api.minimaxi.com/v1` |
| Token Plan Key | `sk-cp-...` | 订阅套餐（**c**oding **p**lan） | **Anthropic 协议** | `api.minimaxi.com/anthropic` |

两类 key **互不通用**。MiniMax 把 Coding Plan 当作 Claude Code / Cursor 的替代订阅在卖，所以**只发布 Anthropic 协议入口**，文档里所有调用示例都是 `anthropic.Anthropic()`。

### 2.4 解决方案：双协议工厂 + 调用侧零感知

**最 naive 的做法**是把所有 `ChatOpenAI` 改成 `ChatAnthropic`。但这样：
- 5 个 chain 文件都要动
- 未来切回 OpenAI 还要再改一遍
- 业务代码绑死具体实现，违反依赖倒置

**正确做法**用 LangChain 的 `BaseChatModel` 抽象——所有具体实现都遵守同一个接口（`invoke / ainvoke / stream / astream`），可通过 `prompt | llm | parser` 管道组合。

```python
# backend/app/core/llm.py
def get_chat_model(...) -> BaseChatModel | None:
    settings = get_settings()
    provider = settings.active_llm_provider
    if not provider:
        return None
    if provider == "anthropic":
        return ChatAnthropic(api_key=..., base_url=..., model=...)
    return ChatOpenAI(api_key=..., base_url=..., model=...)
```

配置层做"按 key 自动判断 + 显式覆盖"：

```python
@property
def active_llm_provider(self) -> str:
    explicit = self.llm_provider.strip().lower()
    if explicit in {"openai", "anthropic"}:
        return explicit
    if self.anthropic_enabled: return "anthropic"
    if self.openai_enabled:    return "openai"
    return ""
```

→ **5 个业务调用方一行不动**。未来加 Gemini / Bedrock / 千问只动这两个文件。

### 2.5 讲述模板

**30 秒版**：
> "我把一个 MiniMax 订阅套餐 key 误用到了 OpenAI 兼容接口上连续 401。我没有直接重申请，而是写了个探针脚本同时打三个候选域名，发现都返回 MiniMax 的 request_id，确认请求到了集群、是协议错配。MiniMax 的订阅套餐只发布 Anthropic 协议，我用 LangChain 的 BaseChatModel 抽象做了双协议工厂，5 个业务调用点零改动。"

**追问 1：为什么不直接全部换 ChatAnthropic？**
> "调用点耦合到具体类，未来切供应商成本是 O(n)；用 BaseChatModel 抽象层降到 O(1)。这是依赖倒置原则的应用。"

**追问 2：OpenAI 协议和 Anthropic 协议本质上有什么不同？**
> "OpenAI 的 messages 是扁平 `[{role, content}]`，content 永远是 string。Anthropic 的 content 是 list of blocks——支持 text / image / tool_use / thinking 多种块。所以 ChatAnthropic 流式吐出来的 `chunk.content` 才会是 list，这也是下一个坑（StrOutputParser）的根源。两者在 system message 处理上也不同：OpenAI 把 system 当成 messages 数组首项，Anthropic 是顶层独立字段。"

---

## 三、🔴 核心难点 2：StrOutputParser 静默丢 thinking 块

### 3.1 现象

切到 Anthropic 协议、key 通了之后，前端能看到回答文字，但**思考链气泡永远是空**：折叠头显示"已深度思考 3s"，点开啥都没有。

### 3.2 为什么这个坑特别值得讲

它揭示 **LangChain 抽象层的副作用**——一个"看起来无害"的工具组件其实在默默过滤数据。这种"工具按设计工作但你的需求超出设计意图"的场景比 bug 更难定位。

### 3.3 排查：直接观察流式 chunk 的真实形状

不能猜，直接打印：

```python
async for chunk in (prompt | m).astream({"q": "你好"}):
    print(type(chunk.content).__name__, repr(chunk.content)[:100])
```

输出（去重后）：
```
list  [{'type': 'thinking', 'thinking': '用户要求简短自我介绍...', 'index': 0}]
list  [{'type': 'thinking', 'signature': '14d6beafe938...', 'index': 0}]
str   '\n\n你好！我是 AI 助手...'
```

**两个反常识的发现**：

1. 同一个流里 `chunk.content` 类型在 list 和 str 之间切换。这是 Anthropic 增量协议的设计：reasoning 阶段保留元信息走 list，text 增量节省序列化开销走 str。
2. thinking 块本身有两种形状：
   - `{type:'thinking', thinking:'内容片段', index:0}` — 真正的 delta
   - `{type:'thinking', signature:'...', index:0}` — Anthropic 防伪签名（防用户篡改 reasoning 后再喂回模型）

### 3.4 根因：StrOutputParser 内部只拼 `text` 块

去看 `langchain_core.output_parsers.string.StrOutputParser` 源码，本质：

```python
def parse_result(self, result):
    return result[0].text  # 不是 .content！是 .text！
```

`AIMessage.text` 在 langchain-core 里**只拼接 `type=='text'` 的块**——所有 thinking / tool_use / image 块都不进入 .text。设计意图是"下游用户拿到的永远是干净的可显示文本"，**代价是丢失元数据**。

它没有 bug，它的**语义就是这样**。坑在于文档没强调，需要看源码或自己探针才知道。

### 3.5 解决方案：去掉 parser，自己分流

```python
async def _stream_chunk_to_streamer(chunk_content, streamer) -> str:
    visible = ""
    if isinstance(chunk_content, list):
        # Anthropic 流式：list of blocks
        for block in chunk_content:
            if not isinstance(block, dict): continue
            t = block.get("type")
            if t == "thinking":
                txt = block.get("thinking", "")
                if txt: await streamer.push_thinking(txt)
            elif t == "text":
                txt = block.get("text", "")
                if txt:
                    visible += txt
                    await streamer.push_token(txt)
            # 其他 type（tool_use、signature）静默忽略
    elif isinstance(chunk_content, str):
        # OpenAI 协议 / Anthropic text 增量
        if chunk_content:
            visible = chunk_content
            await streamer.push_token(chunk_content)
    return visible
```

**协议无关**——同时支持 ChatOpenAI（永远 str）和 ChatAnthropic（混合 list/str）。SSE 通道层加 `push_thinking` 事件类型，前端走独立 `onThinking` 回调累加。

### 3.6 讲述模板

**30 秒版**：
> "切到 Anthropic 协议后思考链 UI 一直是空的。我直接打印了流式 chunk 的真实结构，发现 ChatAnthropic 的 chunk.content 在 list（含 reasoning 元信息）和 str（纯文本增量）之间切换，而 LangChain 默认的 StrOutputParser 会按设计丢掉所有非 text 块——它内部调的是 AIMessage.text 属性，.text 只拼接 type=text 的块。我去掉 parser，自己实现一个协议无关的分流函数，把 thinking 走独立的 SSE 通道推给前端。"

**追问：如果未来有 type=tool_call 块呢？**
> "当前实现 `else: continue` 直接忽略——不污染 visible 输出，也不崩。要支持 tool 调用就加一个 `elif t == 'tool_use'` 分支推到 `push_tool_call` 通道。这是为什么我把分流写成函数而不是 if/else 散落在循环里。"

---

## 四、🔴 核心难点 3：SOCKS 代理污染 LLM 直连

### 4.1 现象

明明 key 和域名都对，本地 `httpx` 直连 OpenAI / MiniMax 时却报：

```
httpx.ProxyError / ConnectError: All connection attempts failed
```

但浏览器、curl、`pip install` 都能正常访问外网。

### 4.2 根因

macOS 上很多网络工具（Surge / ClashX / V2Ray 等）在系统 / 用户 shell 注入 `ALL_PROXY=socks5://127.0.0.1:xxxx`。Python 的 `httpx` 默认会读取这些环境变量并尝试走 SOCKS——**但默认安装的 httpx 不带 socks 后端**，于是直接报错。

更隐蔽的是：即使 `pip install httpx[socks]`（或单独装 `socksio`）解决了第一层，**SOCKS 代理本身可能不放行长连接 / SSE 流**，导致 token 推几个就断。

### 4.3 解决方案：双管齐下

**a. 依赖层**：`requirements.txt` 显式带上 `socksio==1.0.0`，让 httpx 在系统配置 SOCKS 时也能正常走代理。

**b. 调试 / 直连场景**：在跑探针脚本时显式 unset：

```bash
unset ALL_PROXY HTTP_PROXY HTTPS_PROXY ; .venv/bin/python script.py
```

**c. 服务层**：后端进程默认不应该被 shell 的 SOCKS 影响——通过 `Makefile` 的 `dev-backend` 入口在启动前 unset 掉，避免开发者本地代理污染服务。

**d. 错误信息**：`get_chat_model` 里把 ChatOpenAI 初始化失败的异常**主动 log**（之前是静默 `return None`，导致看到的是模糊的"请联系管理员配置 OPENAI_API_KEY"）：

```python
except Exception as exc:
    # 不要静默吞掉异常——SOCKS 代理 / 网络问题 / API key 失效都可能从这里冒出来。
    logger.error("聊天模型初始化失败（provider=%s）：%s", provider, exc, exc_info=True)
    return None
```

### 4.4 讲述模板

> "macOS 上的 SOCKS 代理工具会在 shell 注入 ALL_PROXY，httpx 默认尝试走 SOCKS 但不带 socks 后端，于是 LLM 直连失败。这个坑特别隐蔽：浏览器、curl 都正常，只有 Python httpx 中招。解决是三层——依赖里固定带 socksio，开发脚本统一 unset 三个 proxy 环境变量，最关键是把工厂函数里的初始化异常从静默 return None 改成 logger.error 打出来，否则永远看到的是模糊的'未配置 KEY'兜底文案，根本不知道真因。"

---

## 五、🟡 架构决策：后端多依赖优雅回退

### 5.1 问题背景

这个项目演示的是"企业办公助手"，要在不同环境跑：
- 演示电脑：可能没装 Docker、没装 Milvus、没装 PostgreSQL
- 真实部署：完整外部依赖
- CI / 测试：进程内 SQLite + 内存 fake

如果**强依赖外部基础设施**，演示一启动就 502，体验很差。

### 5.2 设计原则：每层依赖都做"探测 + 回退"

| 依赖 | 健康时使用 | 不可用时回退 |
|---|---|---|
| PostgreSQL | 业务数据查询 | SQLite 本地文件 |
| Milvus | 向量检索 | 本地词法检索（BM25）|
| Embedding 模型 | 生成向量 | 跳过向量化、纯关键词检索 |
| 商旅外部接口 | 真实下单 | 本地 fake 订单确认 |
| LLM（双协议任一） | 自然语言回答 | 友好的占位回答 |

判定逻辑都在配置层：

```python
@property
def llm_enabled(self) -> bool:
    return self.active_llm_provider != ""

@property
def has_embedding_model(self) -> bool:
    return bool(self.openai_embedding_model.strip())

@property
def normalized_business_db_backend(self) -> str:
    # auto: 自动探测 PostgreSQL，不通就 SQLite
    ...
```

### 5.3 设计取舍：什么时候**不**做回退

不是所有依赖都该回退。**跨过这条线就要 fail-fast**：
- 鉴权 / JWT 配置缺失 → 启动直接报错（安全敏感）
- 业务数据 schema 不一致 → 报错（数据正确性）

**判断标准**：缺少这个组件后系统给出的回答**是否还诚实**。Embedding 缺失只是检索质量下降、还诚实；JWT 错了用户看到的是错乱的权限，不诚实，必须 fail。

### 5.4 讲述模板

> "项目里有 5 层外部依赖，但要兼容演示电脑、CI、生产三套环境。我的原则是把'体验降级'和'数据错误'分开——前者全部做软回退（PostgreSQL→SQLite、Milvus→词法检索、Embedding 缺失→纯关键词、商旅接口→本地 fake、LLM→占位回答），后者必须 fail-fast（JWT、schema）。判断标准是缺这个组件后系统的回答还诚不诚实——检索质量降可以接受，权限错乱不行。"

---

## 六、🟢 安全工程：首次推 GitHub 脱敏

### 6.1 为什么这个问题不能小看

API key 一旦推上公开仓库，**几分钟内就被自动爬虫扒走**。删 commit 没用——历史 commit 在远端永久存在，必须吊销 key 重发。所以"防"远比"治"重要。

### 6.2 容易漏的位置

| 类别 | 工作区典型路径 | 处置 |
|---|---|---|
| 当前 .env | `.env` | 必 ignore |
| 备份 .env | `.env.bak.<ts>`、`.env.local` | 必 ignore（最容易漏） |
| 调试探针 | `scripts/probe_key.py` | 必 ignore |
| IDE 配置 | `.idea/`、`.vscode/` | 一般 ignore |
| AI 代理本地配置 | `.codex/`、`.windsurf/` | 一般 ignore（可能含 token） |
| 历史测试 fixture | 测试代码里"占位 key" | grep 全文确认 |

### 6.3 双重验证（这是关键加分点）

不要只信 .gitignore 写得对：

```bash
# 1. 全文扫具体 key 片段
grep -rn "sk-cp-Nl\|QH4zMg\|SHwayo" \
  --include='*.py' --include='*.ts' --include='*.vue' --include='*.md' \
  --exclude-dir=node_modules --exclude-dir=.venv .

# 2. 扫 staged diff（git add 之后、commit 之前）
git diff --cached -S "sk-cp-" -S "sk-api-" --stat
```

`-S` 是 git 的 "pickaxe"——**专门搜 commit 中是否引入或删除了某个字符串**。返回 0 命中才能放心 commit。

### 6.4 讲述模板

> "首次推 GitHub 之前我做了一次系统脱敏。除 .env 外专门处理三类容易漏的：迁移脚本生成的 .env.bak.<ts> 备份、调试探针脚本（直接读真 key 打供应商）、IDE 和 AI 代理工具本地配置。验证手段是两步：grep 全文扫具体 key 片段、git diff --cached -S 扫 staged diff，确认 0 命中再 commit。如果不小心推了，立即吊销 key——历史 commit 在远端不可撤销，必须假设 key 已经被爬走。长期肯定要装 pre-commit + trufflehog 自动化。"

---

## 七、🔵 历史前情提要（已修复，索引到现有文档）

下面这些是本次会话之前已经踩过、解决了的问题，每条只列**现象 + 根因一句话**，详细解释看 `@/Users/leila/Documents/coding/tongtong/docs/前端难点笔记.md` 对应章节。

### 7.1 前端流式 / 响应式 4 件套（最值得讲的）

| # | 现象 | 根因 | 看哪节 |
|---|---|---|---|
| 1 | 助手回复显示 `[object Promise]` | marked v15+ 默认异步，返回 Promise；v-html 拿到的是 `[object Promise]` | 前端难点笔记 §1 |
| 2 | EventSource 连流后端有 token 但前端空白 | 浏览器原生 EventSource **只支持 GET**，最初只暴露 POST | 前端难点笔记 §2 |
| 3 | push 进数组后改对象 UI 不刷新 | Vue 3 的 reactive 是 Proxy，push 后必须**从数组里再取出来**改才会触发更新 | 前端难点笔记 §3 |
| 4 | `<think>...</think>` 流式中渲染断片闪烁 | 流式还没到 `</think>` 时，`<details>` 被反复重建 → 加"未闭合中间态"分支兜底 | 前端难点笔记 §4 |

**讲述要点**：1 反映"升级第三方库要看 changelog"；2 反映"协议选型要先看浏览器 API 限制"；3 是 Vue 3 响应式机制的最常见误用；4 是流式 UI 的边界条件意识。

### 7.2 SSE 鉴权（GET 不能传 Authorization header）

EventSource 不能自定义 Header，所以 Authorization Bearer 没法走头部。最终方案：把 `access_token` 放到 query string 里，后端在 GET 端点专门兼容。**安全代价**：token 进了浏览器历史 / 服务器访问日志，所以必须 **短生命周期 + HTTPS**。

详见 `@/Users/leila/Documents/coding/tongtong/frontend/src/composables/useChatStream.ts:18-24` 和 前端难点笔记 §10。

### 7.3 后端启动相关 4 件套

| # | 现象 | 根因 | 看哪节 |
|---|---|---|---|
| 1 | `Address already in use` 启动失败 | 上次 uvicorn 没正常退出，8000 端口有残留进程 | 后端启动手册 §12.1 + Makefile `kill-backend` |
| 2 | 启动卡在 `Waiting for application startup.` | 依赖中某个 lifespan handler 死锁（通常是同步 IO 在 async） | 后端启动手册 §12.2 |
| 3 | 改了 `.env` 但行为没变 | `get_settings()` 用了 `lru_cache`，进程内只解析一次 | 后端启动手册 §12.4 |
| 4 | 模型一调就 404 | base_url 写错或模型名已下线 | 后端启动手册 §12.3 |

### 7.4 antd / DOMPurify / Vite 各 1 个

| # | 现象 | 根因 |
|---|---|---|
| 1 | antd 主题色配了按钮还是默认蓝 | 主题需要在 `<a-config-provider :theme>` 顶层注入，仅设 CSS 变量不够 |
| 2 | `<details>` 折叠块被吃掉 | DOMPurify 默认禁未知标签，需 `ADD_TAGS: ['details', 'summary']` |
| 3 | Vite HMR `connection lost` | 升级 vite 后 dev server 默认端口冲突，要么调端口要么 disable HMR |

详见 前端难点笔记 §8 §9 §11 §12。

### 7.5 后端意图分类把闲聊也走业务

闲聊"你好"被分类到知识库 RAG，硬要找文档片段，回答僵硬。**修法**：在 intent 节点加置信度阈值 + `chitchat` 兜底意图，意图模糊时直接走通用 LLM 回答。详见 前端难点笔记 §13。

---

## 八、⚪ 轻量优化：思考链 / 打字光标 / 时间戳

不算难点，但是用户感知很强的 UX 细节。每件事一段话带过：

### 8.1 豆包风格思考链折叠

每条助手消息加 5 个状态字段（`isThinking` / `thinking` / `thinkingStartAt` / `thinkingEndAt` / `thinkingExpanded`）。**思考阶段默认展开实时滚动文字**，第一个正式 token 到达时切 `isThinking=false` + 自动折叠。秒数实时跳动用一个 250ms 的 reactive 时钟驱动 computed，`watch(isThinking)` 控制定时器生命周期，加 `onBeforeUnmount` 兜底防内存泄漏。详见 `@/Users/leila/Documents/coding/tongtong/frontend/src/components/ChatMessageBubble.vue`。

### 8.2 流式打字光标

思考折叠后到流彻底 done 之间，用户会以为已经停了。在气泡尾部加一个闪烁竖条，仅在 `isStreaming && !isThinking` 时显示：

```css
.typing-cursor { animation: typing-blink 1s steps(2, start) infinite; }
@keyframes typing-blink { to { visibility: hidden; } }
```

用 `steps(2, start)` 而不是 `linear`——前者是硬切，还原终端光标；后者是渐变，看着像呼吸灯廉价感。颜色用 `currentColor` 跟随气泡主题，主题切换不用改样式。

### 8.3 时间戳挪到气泡外

老布局把时间贴在气泡内部，跟正文抢视觉权重。改成 flex column：气泡和时间上下排，`align-items` 控制左右对齐（用户消息靠右、助手消息靠左），数字字体加 `tabular-nums` 避免秒数跳动时左右抖。

---

## 九、回头看的工程教训

### 9.1 第三方 API key 别按域名认协议

同一供应商可能同时提供 OpenAI / Anthropic / 自家原生三套接口，订阅类 key **经常只支持其中一种**。看文档要找"调用示例"那一节看用哪个 SDK。

### 9.2 看似无害的胶水层可能在丢数据

`StrOutputParser` 看似只是"取个字符串"，其实过滤了 message content 里的非 text 块。这种"工具按设计工作但你的需求超出设计意图"的场景比 bug 更难发现。**写 chain 时多打几次原始 chunk**，比看官方 README 学到的多。

### 9.3 静默吞异常是头号杀手

以前 `get_chat_model` 失败时 `return None`，业务降级到"未配置 KEY"占位文案。但实际原因可能是 SOCKS 代理 / 域名错 / key 失效——**全部被一个静默 None 掩盖**。改成 `logger.error(..., exc_info=True)` 后，定位时间从 30 分钟变 30 秒。

### 9.4 协议层抽象的价值要在切换时才显现

平时写 `ChatOpenAI` 跟写 `BaseChatModel` 没区别，但这次切 Anthropic 时 5 个调用点零改动——**这就是抽象层最 underrated 的价值**。一开始多写一行抽象，未来省下的不是代码，是"是否敢切"的心理成本。

### 9.5 流式 UI 必须有三层视觉锚

思考阶段（秒表 + 转圈 + 文字滚动）→ 出字阶段（光标闪烁）→ 全程（按钮禁用）。缺一层用户都会怀疑系统挂了。

### 9.6 安全脱敏的"防"远比"治"成本低

key 推上公开仓库后无法挽回，必须吊销重发。所以 commit 前的 5 分钟 grep + `git diff --cached -S` 验证，省掉一辈子的尴尬。

---

## 十、FAQ 快速翻阅卡

| 问题 | 1 句话答案 |
|---|---|
| 怎么定位 401 是 key 错还是协议错？ | 同一 key 打多个候选域名，看返回是不是供应商自家 request_id |
| 为什么不直接全部换 ChatAnthropic？ | 调用点耦合到具体类，未来切供应商成本是 O(n)；BaseChatModel 抽象降到 O(1) |
| StrOutputParser 为什么丢 thinking？ | 它内部调 `AIMessage.text`，.text 只拼 type='text' 块，是设计意图不是 bug |
| ChatAnthropic 的 chunk.content 为啥时而 list 时而 str？ | reasoning 阶段保留元信息走 list，text 增量节省序列化走 str |
| 本地 LLM 直连失败、curl 又正常？ | 系统 SOCKS 代理污染 httpx，依赖加 socksio + 开发脚本 unset proxy |
| 后端遇到外部依赖缺失怎么办？ | 区分"体验降级"和"数据错误"——前者软回退，后者 fail-fast |
| EventSource 鉴权怎么传？ | 不能自定义 Header，token 走 query string + 短生命周期 + HTTPS |
| Vue 3 push 进 ref 数组后改不动？ | push 后从数组重取 reactive proxy 来改，原引用失效 |
| 推 GitHub 怎么防泄漏 key？ | grep 全文 + `git diff --cached -S` 双重验证，pre-commit 挂 trufflehog |

---

## 十一、关键文件索引

| 主题 | 路径 |
|---|---|
| 双协议工厂 | `backend/app/core/llm.py` |
| 配置字段 + 自动判断 | `backend/app/core/config.py` 43-60、128-155 |
| chunk 分流 | `backend/app/chains/response_chain.py` 13-41、82-97 |
| SSE thinking 通道 | `backend/app/core/streaming.py` 28-30 |
| 前端流式钩子 | `frontend/src/composables/useChatStream.ts` |
| 思考链 UI | `frontend/src/components/ChatMessageBubble.vue` |
| 消息状态机 | `frontend/src/pages/WorkspacePage.vue` 159-180、202-239 |
| 类型定义 | `frontend/src/types/index.ts` 17-35 |
| 一次性迁移脚本 | `scripts/migrate_env_to_anthropic.py` |
| 配置模板 | `.env.example` |
| 忽略规则 | `.gitignore` |
| **历史前端坑** | `docs/前端难点笔记.md`（13 个详条目） |
| **后端启动报错** | `docs/后端启动手册.md` §12（4 个常见报错） |
